<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.lib.system.repository.LibraryManagementSystem">
	<resultMap id="result" type="com.lib.system.entity.Book">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="category" column="category" />
		<result property="author" column="author" />
		<result property="produceYear" column="produce_year" />
		<result property="bookType" column="book_type" />
	</resultMap>

	<!-- Add Book -->
	<insert id="save" parameterType="com.lib.system.entity.Book">
		INSERT INTO book
		(id,name,category,author,produce_year,book_type)
		VALUES
		(#{id},#{name},#{category},#{author},#{produceYear},#{bookType})
	</insert>

	<select id="getAllBook" resultMap="result">
		SELECT * FROM book
	</select>
	
	<select id="findByAuthor" parameterType="string" resultMap="result">
		SELECT * FROM book
		where author = #{author} 
	</select>
	
	<select id="findByName" parameterType="string" resultMap="result">
		SELECT * FROM book
		where name = #{name} 
	</select>
	
	<select id="findById" parameterType="int" resultMap="result">
		SELECT * FROM book
		where id = #{id} 
	</select>
</mapper>
